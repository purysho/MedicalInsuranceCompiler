{
  "name": "medicalinsurancecompiler",
  "private": true,
  "version": "0.1.0",
  "engines": { "node": ">=20.0.0", "npm": ">=10.0.0" },
  "workspaces": ["api", "ui"],
  "scripts": {
    "build": "cd ui && npm install --include=dev && npm run build && mkdir -p ../api/public && cp -r dist/. ../api/public && cd ../api && npm install --include=dev && npm run build",
    "start": "npm -w api start"
  }
}

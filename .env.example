# Required — point to any OpenAI-compatible API
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-...

# Optional — defaults to "auto"
ALICE_MODEL=gpt-4o

# Optional — defaults to 8787
PORT=8787
